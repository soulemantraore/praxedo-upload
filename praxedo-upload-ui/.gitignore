node_modules
dist
dist-ssr
coverage
*.local
.env
.env.local
.DS_Store
*.log

# Ce composant utilise npm (package-lock.json). On ignore les lockfiles
# concurrents pour eviter deux sources de verite sur les dependances.
pnpm-lock.yaml
yarn.lock
